server {
    listen 8080;
   server_name hello.local;

    # Tell Nginx and Passenger where your app's 'public' directory is
    root /u01/{{VMUSER}}/hello/public;

    # Turn on Passenger
    passenger_enabled on;
    passenger_ruby /usr/bin/ruby;
}

